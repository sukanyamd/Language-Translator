<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/bootstrap.min.css">
</head>
<body>
    <form action="/translate" method="POST">
        <div class="card" style="width: 24rem;">

            <div class="card-body">
                <h5 class="card-title">Enter Text to convert to Malayalam</h5>
                <p class="card-text">
                    <textarea name="text" placeholder="Enter Text" cols="20"></textarea>
                </p>

                <button type="submit" class="btn btn-primary">convert</button>
            </div>
        </div>


</form>
<% if (translation) { %>
    <h2>Translation :</h2>
    <p>
        <%= translation %>
    </p>
<% } %>

<% if (error) { %>
    <p>
    <%= error %></p>
<% } %>
</body>
</html>